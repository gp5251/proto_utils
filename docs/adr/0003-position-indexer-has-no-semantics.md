# 编辑器位置数据由无语义索引层提供

go-to-def、语义高亮等编辑器功能的数据来自一个薄位置索引层:只扫描定义点(名字 + range),不解析 import、不做类型判断、不验证合法性。proto-loader 是唯一有权判定 proto 文件对错的组件。刻意禁止索引层做语义,是为了让两个组件永远不会对同一文件给出矛盾结论——这是 ADR-0002 统一语义前端的配套约束,未来不要给索引层「补上」类型解析。
